# .env.example
# This is a template for environment variables used in the application.
# Copy this file to .env.development, .env.test, or .env.production and fill in the values.
# Do not commit sensitive data (e.g., API keys, passwords) to version control.

# Node.js environment (development, test, production)
NODE_ENV=development

# Port for the Express server
# Example: 3000 for development, 8080 for production
PORT=3000

# MongoDB connection URI
# Example: mongodb://localhost:27017/myapp_dev (local) or mongodb+srv://user:pass@cluster0.mongodb.net/myapp_prod (cloud)
MONGODB_URI=your-mongodb-uri

# Redis connection settings
# REDIS_URL: Full Redis connection string (preferred)
# Example: redis://localhost:6379
REDIS_URL=redis://your-redis-host:6379
# REDIS_HOST and REDIS_PORT: Fallback for explicit host/port configuration
REDIS_HOST=your-redis-host
REDIS_PORT=6379

# Application host for generating URLs (e.g., password reset links)
# Example: localhost:3000 (development), yourapp.com (production)
HOST=your-app-host

# SMTP settings for sending emails (e.g., via Nodemailer)
# SMTP_HOST: SMTP server hostname
# Example: smtp.gmail.com (Gmail), smtp.sendgrid.net (SendGrid)
SMTP_HOST=your-smtp-host
# SMTP_PORT: SMTP server port
# Example: 587 (TLS), 465 (SSL)
SMTP_PORT=587
# SMTP_SECURE: Enable SSL/TLS (true/false)
# Example: false for TLS, true for SSL
SMTP_SECURE=false
# SMTP_SERVICE: Email service provider
# Example: gmail, sendgrid, ethereal
SMTP_SERVICE=your-smtp-service
# SMTP_USER: SMTP authentication username
# Example: your-email@gmail.com (Gmail), apikey (SendGrid)
SMTP_USER=your-smtp-user
# SMTP_PASS: SMTP authentication password or API key
# Example: Gmail app password, SendGrid API key
SMTP_PASS=your-smtp-password
# SMTP_FROM: Sender email address
# Example: "Your App <no-reply@yourapp.com>"
SMTP_FROM="Your App <your-smtp-from-email>"

# JWT secret for signing authentication tokens
# Example: A long, random string (at least 32 characters)
JWT_SECRET=your-jwt-secret

# Paystack secret key for payment processing
# Example: sk_test_xxxxxxxxxxxxxxxx (test), sk_live_xxxxxxxxxxxxxxxx (production)
PAYSTACK_SECRET_KEY=your-paystack-secret-key

# Default currency for transactions
# Example: NGN (Nigerian Naira), USD
DEFAULT_CURRENCY=NGN

# Admin credentials for create-admin.js script
# ADMIN_EMAIL: Admin user email
# Example: admin@yourapp.com
ADMIN_EMAIL=your-admin-email
# ADMIN_PASSWORD: Admin user password (will be hashed)
# Example: A strong password
ADMIN_PASSWORD=your-admin-password